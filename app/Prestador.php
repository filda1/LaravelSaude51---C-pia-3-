<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Prestador extends Model
{
   /* public function scopeSearch($query, $data){
        $name= $data->nome;
        $email= $data->morada;
        $type= $data->localidade;
        return $query->where('nome', 'LIKE', "%$name%")-> where('morada', 'LIKE', "%$email%")-> where('localidade', 'LIKE', "%$type%");
      }*/

    
}
